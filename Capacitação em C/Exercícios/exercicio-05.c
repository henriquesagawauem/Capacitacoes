#include <stdio.h>

int main()
{
                float nota1, nota2, nota3;

                scanf("%f  %f %f", &nota1, &nota2, &nota3);
                printf("%.2f\n", (nota1+ nota2 + nota3) / 3);

                return 0;
}